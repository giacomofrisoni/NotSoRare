const user = 'notsorare.app@gmail.com';
const password = '.RareDiseases.';
const service = '@gmail';
const from = 'NotSoRare <' + user + '>';

module.exports = {
    user,
    password,
    service,
    from
};